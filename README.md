Put the inputs to `intpus/` with format like:
```
inputs
├── day01_example.txt
└── day01.txt
```

Run by:
```bash
cargo run --bin run -- 1 example
```
for example files, and
```bash
cargo run --bin run -- 1
```
for regular files.
